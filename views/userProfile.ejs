<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= user.name %> <%= user.surname %> Profile</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"
    />
    <style>
      .profile-background::before {
        content: '';
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 50%;
        background: url('/uploads/<%= user.cover %>') no-repeat center center
          fixed;
        background-size: cover;
        z-index: -1;
      }
    </style>
  </head>
  <body class="bg-gray-100 profile-background" <% if (user.photo) { %>
    style="background: url('/uploads/<%= user.cover %>') no-repeat center center
    fixed; background-size: cover;"> <% } %>

    <div class="min-h-screen flex items-center justify-center">
      <div class="bg-white rounded-3xl shadow-md p-16">
        <h1 class="text-3xl font-semibold text-gray-700 text-center mb-6">
          <%= user.name %> <%= user.surname %>'s Profile
        </h1>
        <div>
          <% if (user.photo) { %>
          <div class="mb-4">
            <img
              src="/uploads/<%= user.photo %>"
              alt="<%= user.name %> <%= user.surname %>'s Photo"
              class="w-32 h-32 rounded-full border-4 border-white shadow-lg mx-auto object-cover object-center"
            />
          </div>
          <% } else { %>
          <div class="mb-4 bg-gray-500 w-32 h-32 rounded-full mx-auto"></div>
          <% } %>
          <h2 class="text-2xl font-semibold text-gray-900 mb-2">
            <%= user.name %> <%= user.surname %>
          </h2>
          <p class="text-lg text-gray-600"><%= user.age %> years old</p>
        </div>
        <div class="mt-4 flex justify-between">
          <a
            href="/all"
            class="py-2 px-4 bg-gray-900 text-white font-semibold rounded-md transform transition-transform hover:bg-gray-800"
          >
            Go back
          </a>
          <div class="flex space-x-2">
            <a
              href="/user/<%= user.id %>/edit"
              class="text-gray-700 hover:text-gray-900 py-2 px-4 font-medium"
            >
              Edit
            </a>
            <a
              href="/user/<%= user.id %>/delete"
              onclick="return confirm('Are you sure you want to delete this user?')"
              class="border-2 border-red-500 bg-white hover:border-red-600 text-red-500 font-semibold py-2 px-4 rounded-md transform transition-transform"
            >
              Delete
            </a>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
